% ---- Apgabals (inequality input) ----
x1_area = "<var(x1_area)>";
x2_area = "<var(x2_area)>";

b = code_helpers('sys2d_area_bounds', x1_area, x2_area);
x1min = b(1); x1max = b(2);
x2min = b(3); x2max = b(4);
